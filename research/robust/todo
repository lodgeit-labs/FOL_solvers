
# graph visualization


- programmatic layout
- visual overview of whole model


the database is a hairball. We can specify what we want to see

0) by simple subgraph-ing
1) declaratively, by asserting statements pointing to some nodes or edges as more important for particular views
2) by crafting queries
3) imperatively, by generating view graph from data

## graph layout
...



## graph abstraction (in browsing/visualization):
	collapsing of multiple equivalent (subsequent) children of a node (of proof tree) into a "repeat" node




## pathfinding in graphs
metaphactory
agraph
neo4j
- at any case, curating of predicates and their semantics is needed



## AI answering over graphs






# pyco2

* revive pyco2, ensure testcase runs to completion
* make output machine-readable - streaming quads first, maybe add binary option later
* rendering of reasoner trace - proof trace - tracer1 web app
* rendering of reasoner trace - proof search (qt?)

* change pyco2 to work with uri's
* pyco2 wrapper / runner for generation of input kb
* extend test2b to reason over a minimal realistic input from excel ic template - 
this poses some interesting questions about ordering of items in lists, about concatenantion of multiple lists of s_transactions. - 
say, we run a variant where we dont enforce ordering of stransactions - 
of forward-searching, variants are produced.
an abstracting visualization might subsequently abstract away the equivalent variants, but where do the variants pop up?:
in 'fr' - 'list exists'?  - nope
  
* pyco 11 - js/bun interpreter - parallel search



# general accounting data stuff 

* additional constraints
- is every transaction in exactly one period


* unified formula language


